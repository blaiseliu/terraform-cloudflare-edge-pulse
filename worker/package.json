{
  "name": "edge-pulse-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "eval": "npx tsx eval/eval.ts"
  },
  "dependencies": {
    "@extractus/feed-extractor": "^7.1.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250502.0",
    "tsx": "^4.21.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.50.0"
  }
}
